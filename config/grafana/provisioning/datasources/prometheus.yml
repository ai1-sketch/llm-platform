# llm-platform — مصدر بيانات Grafana (النوع الثاني، ADR-031). provisioned ككود (لا إعداد يدوي).
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
