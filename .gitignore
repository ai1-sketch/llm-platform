# llm-platform — أسرار وبيانات وملفات ضخمة لا تُرفع أبداً (R-ARCH-02/42)
config/env/.env
*.override.yml
*.override.yaml
# نسخ pg_dump احتياطية — تحوي بيانات/مفاتيح، محلية فقط
backups/

# موديلات وأوزان كبيرة (تُنزَّل محلياً، خارج git)
models/
models-gemma4/
models-embeddings/
wheels/

# بيئات افتراضية (قابلة لإعادة الإنشاء)
.venv/
.venv-*/

# Python cache
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/

# سجلّات/كاش أدوات (نموذج legacy)
*.log
.gradio/
gradio_cached_examples/
