# خدمة الذاكرة — إصدارات مُثبَّتة بدقّة (حتمية البناء، ADR-008 تقوية). للترقية: حدّث + اختبر.
fastapi==0.138.0
uvicorn[standard]==0.49.0
asyncpg==0.30.0
httpx==0.28.1          # عميل التضمين عبر البوّابة (ADR-019/023)
